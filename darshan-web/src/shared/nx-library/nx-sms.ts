import * as http from "http";
import { Promise } from "mongoose";
import * as rm from "typed-rest-client/RestClient";

class NxSms {
  public sendSms(mobile: number, otp: number) {}
}

export default new NxSms();
